### filter-experiment: effect of long-running smudge filters in `git clone` vs. `gix clone`

This investigates how smudge (checkout) process filters interact with executable permissions in `git clone` and `gix clone`.
