#!/bin/sh
set -e
printf 'git-filter-client\nversion=2\n\n'
while IFS= read -r line; do
    test -n "$line" || break
done
printf 'status=success\n\n'
while IFS= read -r line; do
    printf 'Got: %s' "$line" >&2
    cat  # Pass through until EOF.
    printf 'status=success\n\n'
done
